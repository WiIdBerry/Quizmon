{
  "name": "quizmon-beta",
  "private": true,
  "version": "1.0.0-patch-c.1",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check data.js && node --check cosmetics.js && node --check i18n.js && node --check core-utils.js && node --check storage.js && node --check service-worker.js && npm test"
  },
  "engines": { "node": ">=20" }
}
